# @spotterjs/mcp

MCP server exposing spotterjs desktop automation, optional Android ADB automation,
workspace file I/O, and shell execution.

See [docs/MCP.md](../../docs/MCP.md) for configuration and tool list.

```bash
npm run build -w @spotterjs/mcp
npx spotterjs-mcp
```

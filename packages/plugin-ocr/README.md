# @spotterjs/plugin-ocr

**Preview / placeholder** — not a production OCR solution in v0.1.0.

`useOcrPlugin()` throws at runtime. Tesseract was removed after evaluation; PaddleOCR integration is planned.

For WeChat-style automation today, use template matching via `@spotterjs/core` (see repo `scripts/integration/`).

## License

Learning and non-commercial use are free. Commercial use requires authorization — see [LICENSE](../../LICENSE) or contact `ypl123698745@qq.com`.
